package co.edu.unbosque.centroadoptivo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Pruebas de integración para la clase principal de la aplicación del centro adoptivo.
 * <p>
 * Verifica que el contexto de Spring Boot se cargue correctamente con todas
 * sus configuraciones, beans y dependencias al iniciar la aplicación.
 * </p>
 */
@SpringBootTest
class CentroadoptivoApplicationTests {

    /**
     * Verifica que el contexto de la aplicación Spring Boot se inicializa
     * sin errores al arrancar.
     */
    @Test
    void contextLoads() {
    }
}
